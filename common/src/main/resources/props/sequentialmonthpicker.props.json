{
    "type": "object",
    "additionalProperties": false,
    "required": [
        "month",
        "year",
        "dateFormat"
    ],
    "properties": {
        "month": {
            "type": "number",
            "description": "The year of the month to display.",
            "default": 7
        },
        "year": {
            "type": "number",
            "description": "The month to display.",
            "default": 2025
        },
        "dateFormat": {
            "type": "string",
            "description": "the format of the selected month.",
            "default": "LLLL yyyy"
        },
        "preventPast": {
            "type": "boolean",
            "description": "If true, prevents the user from selecting a month in the past.",
            "default": false 
        },
        "preventFuture": {
            "type": "boolean",
            "description": "If true, prevents the user from selecting a month in the future.",
            "default": false 
        },
        "disabled": {
            "type": "boolean",
            "default": false,
            "description": "If the picker is disabled."
        },
        "style": {
            "$ref": "urn:ignition-schema:schemas/style-properties.schema.json",
            "default": {
                "classes": ""
            }
        }
    }
}
